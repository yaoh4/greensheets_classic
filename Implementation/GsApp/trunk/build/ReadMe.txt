= Build Properties =

Copy user.properties_template to build.properties, and set the proper values for your environment.

Do not commit user.properties to the repository.  The easiest way to do this is to add it to the SVN
ignore list.